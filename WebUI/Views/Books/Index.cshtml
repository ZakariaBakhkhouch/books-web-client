@using Domain.Models.Books;
@model IList<BookModel>;
@{
    Layout = "_Layout";
    ViewData["Title"] = "List of Books";
}

<h1>List of Books :</h1>
<div class="table-responsive">
    <br />
    <a asp-action="Create" class="btn btn-primary">Add New Book</a>
    <br />
    <br />
    <table class="table table-striped table-sm table-hover table-bordered border-dark">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Title</th>
                <th>Genre</th>
                <th>Description</th>
                <th>Date Added</th>
                <th>Rate</th>
@*                <th>Book Cover</th>
*@                <th>Publisher Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Title</td>
                    <td>@item.Genre</td>
                    <td>@item.Descriprtion</td>
                    <td>@item.DateAdded</td>
                    <td>@item.Rate</td>
                    @*<td>
                        @if (item.Cover != null)
                        {
                            <img id="bookCover" style="width:250px;height:250px; object-fit:cover"
                            src="data:image/*;base64,@(Convert.ToBase64String(item.Cover))">
                        }
                        else
                        {
                            <img id="bookCover" style="width:150px;height:150px; object-fit:cover" src="">
                        }
                        
                    </td>*@
                    <td>@item.PublisherId</td>
                   @* @if(User.Identity.IsAuthenticated == true && User.IsInRole(UserRoles.Admin))
                    {
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Edit</a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                        </td>
                    }*@
                    <td>
                        <a asp-action="Edit"   asp-route-id="@item.Id" class="btn btn-primary">Edit</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>